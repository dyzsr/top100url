#include "../include/input.h"
#include "../include/output.h"
#include <cstdio>
#include <cstdlib>
#include <cstring>

const int MAX_URL_LEN = 65536;

int main() {
	Input in(stdin, 256);
	Output out(stdout, 256);
	// Partition
	return 0;
}
