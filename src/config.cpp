#include "../include/config.h"

size_t MAX_URL_SZ = 65535;
size_t MAX_FNAME_SZ = 40;

size_t FILE_SZ = (1 << 26);

size_t MERGE_NUM = 10;

size_t PARALLEL_NUM = 8;

size_t BUF_SZ_L = (1 << 26);
size_t BUF_SZ_M = (1 << 24);
size_t BUF_SZ_S = (1 << 20);
